spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/FullStackDevelopment
    username: postgres
    password: yash
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    
server:
  port: 15000
